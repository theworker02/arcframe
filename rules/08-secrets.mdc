---
description: Never return secret values; expose env key names only.
alwaysApply: true
---

# 08 — Secrets hygiene

Never return secret values from env tools. Never expose database credentials. Prefer `.env.example` key names only.
