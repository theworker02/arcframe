---
title: Arc Graph
---

# Arc Graph

Edge types include `IMPORTS`, `DEPENDS_ON`, `CONTAINS`, `TESTS`, `ROUTES_TO`.

```bash
arc graph build
arc graph stats
arc graph neighbors <node>
```

Confidence is attached per edge (confirmed vs inferred).
