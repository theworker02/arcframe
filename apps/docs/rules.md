---
title: Rules
---

# Rules

Repo pack under `rules/` as Open Plugins `.mdc` files (01–20). Copied into `.arcframe/rules/` on `arc init` (`.md` / `.mdc`).

Themes: local-first, evidence, incremental, one-engine, safe automation, cross-platform, Cursor API honesty, secrets hygiene.

These root files are the canonical Open Plugins rule pack discovered by Cursor when installing this GitHub repo as a plugin.
